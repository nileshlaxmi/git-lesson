# Unix commands for file

```Bash
touch filename # it will create a file

ls -al # it will list all files with hidden files

cat file1>>file2 # it will append file1 in file2

cd foldername # it will move inside that folder

rm -rf foldername # it will delete all files present inside that folder recursively

```
